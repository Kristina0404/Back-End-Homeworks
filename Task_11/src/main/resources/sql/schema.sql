create table events (
                        id identity primary key,
                        title varchar(50),
                        startDate varchar(10),
                        endDate varchar(10)
);